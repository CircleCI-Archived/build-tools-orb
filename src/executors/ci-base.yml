parameters:
  tag:
    type: string
    default: latest

# Image with as few packages installed as possible, while still being usable
# for builds: https://github.com/cibuilds/base.
resource_class: small
docker:
  - image: cibuilds/base:<< parameters.tag >>
